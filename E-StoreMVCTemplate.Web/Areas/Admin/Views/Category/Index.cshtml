@model List<GetCategoryListDto>

@{
    ViewData["Title"] = "Categories";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}


<div class="container-fluid px-4">

    <!-- Header + Button -->
    <div class="d-flex justify-content-between align-items-center mt-4 mb-4">
        <div>
            <h1>Categories</h1>
            <span class="badge bg-secondary">Total Categories: @(Model?.Count ?? 0)</span>
        </div>

        <a asp-area="Admin"
           asp-controller="Category"
           asp-action="Create"
           class="btn btn-primary">
            + Create Category
        </a>
    </div>

    <!-- Table -->
    <div class="card mb-4">
        <div class="card-body">

            <table class="table table-bordered table-hover">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>DisplayOrder</th>
                        <th>ProductCount</th>
                        <th>SubCategories</th>
                        <th>Action</th>
                    </tr>
                </thead>

                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @item.Name
                                </td>
                                <td>
                                    @item.DisplayOrder
                                </td>
                                <td>
                                    @item.ProductCount
                                </td>
                                <td>
                                    @if (item.SubCategory != null && item.SubCategory.Any())
                                    {
                                        foreach (var sub in item.SubCategory)
                                        {
                                            <span class="badge bg-info">@sub.Name</span>
                                        }
                                    }
                                    else
                                    {
                                        <span class="text-muted">-</span>
                                    }
                                </td>

                                <td class="text-end">

                                    <a asp-area="Admin"
                                       asp-controller="Category"
                                       asp-action="Detail"
                                       asp-route-id="@item.Id"
                                       class="btn btn-sm btn-info">
                                        Detail
                                    </a>

                                    <a asp-area="Admin"
                                       asp-controller="Category"
                                       asp-action="Update"
                                       asp-route-id="@item.Id"
                                       class="btn btn-sm btn-warning">
                                        Update
                                    </a>

                                    <a asp-area="Admin"
                                       asp-controller="Category"
                                       asp-action="Delete"
                                       asp-route-id="@item.Id"
                                       class="btn btn-sm btn-danger"
                                       onclick="return confirm('Are you sure you want to delete it?')">
                                        Delete
                                    </a>
                                </td>

                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="5" class="text-center text-muted">
                                No Categories found
                            </td>
                        </tr>
                    }
                </tbody>

            </table>

        </div>
    </div>

</div>
